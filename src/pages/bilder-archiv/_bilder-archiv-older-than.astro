---
import type { TypeMediaFields } from 'src/contentful';
import BilderArchivCard from './_bilder-archiv-card.astro';

const { jahr, media } = Astro.props;
---

<article>
  <h2 class="mt-16">...noch älter</h2>
</article>
<div class="grid grid-cols-1 items-stretch gap-8 md:grid-cols-2 lg:grid-cols-3 lg:gap-4">
  {
    media
      .filter((m: TypeMediaFields) => m.jahr < jahr)
      .map((m: TypeMediaFields) => <BilderArchivCard slug={m.slug} titel={m.titel} vorschaubild={m.vorschaubild} />)
  }
</div>
